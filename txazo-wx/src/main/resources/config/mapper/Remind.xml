<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.3//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.txazo.wx.application.remind.mapper.RemindMapper">

    <insert id="addRemind" parameterType="Remind" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO Remind (title, createTime) VALUES (#{title}, now())
    </insert>

</mapper>